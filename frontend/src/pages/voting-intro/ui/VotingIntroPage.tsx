import { useEffect } from "react";

import { ROUTES } from "../../../shared/config";
import { ModuleIntro } from "../../../widgets/module-intro";

/**
 * 投票・合意形成の導入ページ（"/voting"）。
 * 複数人の参加が前提でサンプル単独では体験が完結しないため、サンプル導線は置かず、
 * CTA は投票の作成のみとする。
 */
export function VotingIntroPage() {
  useEffect(() => {
    document.title = "投票・合意形成 | ennx";
  }, []);

  return (
    <ModuleIntro
      title="投票・合意形成"
      lead="複数案から 1 つを選ぶ意思決定を、投票ルールごとの結果と性質つきで可視化します。匿名の参加 URL を配るだけで、登録なしに参加者を集められます。"
      problems={[
        "多数決で決めたが、少数の支持しか集めていない案が通ってしまう。",
        "投票ルールを変えると結果が変わることに、決めたあとで気づく。",
        "「なぜこの案に決まったのか」を数字で説明できない。",
        "意見を集めるのにアカウント登録やツール導入が必要で、参加者が集まらない。",
      ]}
      scenes={["方針・施策の選択", "イベントの日程・企画決め", "チーム内の優先順位づけ", "候補案の絞り込み"]}
      stepDescriptions={[
        "選択肢と投票ルールを設定して投票を作成し、発行された参加用 URL を配ります。",
        "参加者がニックネームで投票し、締め切ると集計されます。",
        "投票ルールごとの結果と、そのルールが満たす性質・満たさない性質を並べて確認できます。",
        "集計結果を CSV / JSON で書き出し、決定の根拠として共有できます。",
      ]}
      properties={[
        {
          name: "ルールごとの性質の明示",
          description:
            "各投票ルールが理論的に満たす性質・満たさない性質を、結果と一緒に提示します。",
        },
        {
          name: "結果の比較可能性",
          description:
            "同じ投票データを複数のルールで集計し、ルール選択が結果をどう変えるかを確認できます。",
        },
        {
          name: "参加の匿名性",
          description:
            "アカウント登録は不要で、参加用 URL を知っている人がニックネームだけで参加できます。",
        },
      ]}
      ctas={[{ label: "投票を作成する", to: ROUTES.voting.create }]}
    />
  );
}
